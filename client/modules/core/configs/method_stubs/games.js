import {check} from 'meteor/check';

export default function ({Meteor, Collections}) {
  //Meteor.methods({
  //  'games.create'(_id, type, blackId, whiteId, turn, position) {
  //    check(_id, String);
  //    check(type, String);
  //    check(blackId, String);
  //    check(whiteId, String);
  //    check(turn, String);
  //    check(position, String);
  //
  //
  //    const createdAt = new Date();
  //    const game = {
  //      _id, type, blackId, whiteId, turn, position, createdAt,
  //      saving: true
  //    };
  //
  //    Collections.Games.insert(game);
  //  }
  //});
}
